'use client'

import React from 'react'

interface BoxProps {
    children: React.ReactNode;
    className?: string;
}

const Header = () => {
    return (
        <div>Header</div>
    )
}

export default Header