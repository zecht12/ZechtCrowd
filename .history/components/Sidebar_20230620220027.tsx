'use client'

import React, { Children } from 'react'

interface Sidebar {
    children: React.ReactNode;
}

export default function Sidebar() {
    return (
        <div>
            {Children}
        </div>
    )
}
